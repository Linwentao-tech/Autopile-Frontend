{
    "builds": [
        {
            "src": "package.json",
            "use": "@vercel/next",
            "config": {
                "buildCommand": "next build",
                "outputDirectory": ".next/standalone"
            }
        }
    ]
}